Recommended requirements:

+ CPUs = 2
+ Memory = 4GB

Minimum requirements:

+ CPUs = 1
+ Memory = 1GB

Approximate run time: under a minute for typical inputs (tens of thousands of rows).

ARM processor support: True
